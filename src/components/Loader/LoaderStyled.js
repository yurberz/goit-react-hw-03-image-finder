import styled from "styled-components";

const Div = styled.div`
  display: flex;
  justify-content: center;

  .text {
    margin: 0;
    font-size: 32px;
    color: honeydew;
  }
`;

export default Div;
